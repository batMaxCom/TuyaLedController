class DeviceProperty:
    def __init__(
            self,
            code: str,
            type: str,
            value: str

    ):
        self._code = code
        self._type = type
        self._value = value
